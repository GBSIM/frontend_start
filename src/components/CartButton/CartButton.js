import './CartButton.css';

import IconButton from '../IconButton/IconButton';

export default function CartButton() {
    return (
        <IconButton
            width={34}
            heigth={34} />
    )
}